#head.header(:class="{'header--home': $route.path === '/' && !$store.state.menu}")
  v-waypoint.header__waypoint(@waypoint-out='scrollOut', @waypoint-in='scrollIn')
  Menu
  transition(name='home-fade')
    HomeHero(v-show='$route.path === "/" && !$store.state.menu')
