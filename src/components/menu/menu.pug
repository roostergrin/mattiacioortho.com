.menu
  .menu__overlay
  .menu__container
    router-link.menu__link(v-for='(link, i) in links', v-if='link.navigation', :to='link.path', :style="{backgroundImage: 'url(' + link.url + ')'}", :key='i') {{ link.name }}
